{% extends 'shop.html.twig' %}

{% block title %}Nos goodies{% endblock %}

{% block body %}

    <div class="jumbotron">
        <div class="container">
            {{ form_start(form) }}
            <div class="form-row align-items-end">
                <div class="col">
                    {{ form_row(form.minPrice) }}
                </div>
                <div class="col">
                    {{ form_row(form.maxPrice) }}
                </div>
                <div class="col">
                    <div class="form-group">
                        <button class="btn btn-primary">Rechercher</button>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>

    <div class="container-fluid my-4">
        <h1>Nos goodies</h1>
        <div class="row">
            {% for produit in goodies %}
                <div class="card col-xs-12 col-sm-12 col-md-12 col-lg-4 col-xl-2 m-4">
                    <div class="card-body">
                        <br/><img src="/assets/image/produits/{{ produit.filename }}" class="card-img-bottom" alt="{{ produit.description }}">
                        <h4 class="card-title">{{ produit.nom }}</h4>
                        <p class="card-text"> {{ produit.description }}</p>
                        <p class="card-text"><small class="text-muted">Prix : {{ produit.prix }}€</small></p>
                        <div>
                            <a class="btn btn-primary" href="{{ path('boutique/ID', {ID: produit.id, slug: produit.slug}) }}">Voir le produit</a>
                        </div>
                        </br>
                        {% if app.user %}
                        <a href="{{ path('cart_add', {'id': produit.id}) }}" class="btn btn-success">
                                Ajouter au panier
                        </a>
                        {% else %}
                        <a href="#" class="btn btn-danger">
                                Se connecter
                        </a>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}